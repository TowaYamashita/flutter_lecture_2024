import 'package:flutter/material.dart';

class CreditNotation extends StatelessWidget {
  const CreditNotation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Powered by Stratonauts Dog API');
  }
}
